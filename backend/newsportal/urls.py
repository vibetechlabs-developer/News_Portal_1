"""
URL configuration for newsportal app.
This file exists to satisfy Django's import requirements.
"""
from django.urls import path

urlpatterns = [
    # Empty URL patterns - this app doesn't define any URLs
]
